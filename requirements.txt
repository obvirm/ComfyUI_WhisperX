--index-url https://pypi.org/simple --extra-index-url https://download.pytorch.org/whl/cu129

whisperx==3.7.4

# Untuk mengunduh model dari Hugging Face Hub.
huggingface-hub

# Untuk decoding file audio. 'av' adalah dependensi whisperx untuk memuat audio.
av

# Untuk membuat permintaan HTTP (digunakan dalam pengunduh kustom node).
requests

# Diperlukan oleh backend diarization (pyannote.audio) untuk model VAD.
onnxruntime

# Untuk menampilkan progress bar kustom selama proses.
tqdm
